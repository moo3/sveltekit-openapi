# Changesets

Hello and welcome! This folder has been automatically generated by `@changesets/cli`, a build tool that works
with multi-package repos, or single-package repos to help you version and publish your code. You can
find the full documentation for it [in the readme](https://github.com/changesets/changesets/blob/main/README.md)

**How to add a changeset:**

```bash
npx changeset
```

Then select the package, choose the bump type (patch/minor/major), and write a summary.
The changeset file will be committed with your PR. When merged, the release workflow
will open a "Version Packages" PR to bump versions and update the changelog.
